function setup() {
  const graph = createCanvas(640, 480); // create canvas
  graph.parent('graph');  // then allocate to parent div

  background(0);
  fill(255);
}

function draw() {
  ellipse(50, 50, 80, 80);
}
